<% layout('layouts/boilerplate') -%>
<!-- <= success %> -->
<!-- <h1>All Listings</h1> -->

<!-- <style>
  #filters {
    display: flex;
    flex-wrap: wrap;
  }

  .filter {
    text-align: center;
    margin-right: 2rem;
    margin-top: 2rem;
    opacity: 0.7;
  }

  .filter:hover{
    opacity: 1;
    cursor: pointer;
  }

  .filter p{
    font-size: 0.8rem;
  }

  
   @media (max-width: 768px) {
    #filters {
      flex-direction: column;
      align-items: center;
    }

    .filter {
      margin: 0.5rem 0;
    }

    .tax-toggle {
      margin-top: 1rem;
      text-align: center;
      width: 100%;
    }
  }
</style> -->

<!-- <style>
  #filters {
    display: flex;
    flex-wrap: nowrap;
    overflow-x: auto;
    padding: 1rem;
    gap: 2rem;
    scroll-behavior: smooth;
    
  }

  .filter {
    flex: 0 0 auto;
    text-align: center;
    opacity: 0.7;
    transition: 0.3s;
  }

  .filter:hover {
    opacity: 1;
    cursor: pointer;
  }

  .filter p {
    font-size: 0.8rem;
    margin-top: 0.25rem;
  }

  .tax-toggle {
    flex: 0 0 auto;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    white-space: nowrap;
    margin-left: auto;
  }

  /* Hide scroll bar */
  #filters::-webkit-scrollbar {
    display: none;
  }
  #filters {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }

  /* Medium screen (tablets) */
  @media (min-width: 768px) and (max-width: 991px) {
    #filters {
      flex-wrap: wrap;
      justify-content: center;
      overflow-x: visible;
    }

    .filter {
      width: 90px;
    }
  }

  /* Large screen */
  @media (min-width: 992px) {
    #filters {
      flex-wrap: wrap;
      justify-content: flex-start;
      overflow-x: visible;
    }

    .filter {
      width: 90px;
    }
  }

   .listing-card {
    display: block !important;
  }

  .card {
    display: block !important;
  }

  .row .col {
    display: block !important;
  }

  .tax-info{
    display: none;
  }

  .tax-toggle {
    border: 1px solid black;
    border-radius: 1rem;
    height: 2.5rem;
    padding: 1rem;

  }

</style> -->
<style>
/* Container for filters + toggle */
.filter-container {
  display: flex;
  align-items: center;
  overflow-x: auto;
  padding: 1rem 1.5rem;
  gap: 2rem;
  flex-wrap: nowrap;

}


.tax-toggle {
  border: 1px solid #ccc;
  border-radius: 2rem;
  padding: 0.5rem 1rem;
  display: flex;
  align-items: center;
  justify-content: space-between;
  max-width: max-content;
  white-space: nowrap;
}


/* Individual filter styling */
.filter {
  text-align: center;
  flex: 0 0 auto;
  opacity: 0.7;
  transition: 0.3s;
}

.filter:hover {
  opacity: 1;
  cursor: pointer;
}

.filter p {
  font-size: 0.8rem;
  margin-top: 0.25rem;
}

/* Hide scroll bar */
.filter-container::-webkit-scrollbar {
  display: none;
}
.filter-container {
  -ms-overflow-style: none;
  scrollbar-width: none;
}

/* Toggle on the right */
.tax-toggle {
  margin-left: auto;
  white-space: nowrap;
}

/* Responsive tweaks */
@media (max-width: 768px) {
  .filter-container {
    flex-wrap: nowrap;
    overflow-x: auto;
  }

  .tax-toggle {
    margin-left: auto;
  }
}

@media (min-width: 768px) and (max-width: 992px) {
  .filter-container {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter {
    width: 90px;
  }
}

.tax-info {
  display: none;
}


</style>

<!-- <div class="d-flex justify-content-end tax-toggle">
  <div class="form-check-reverse form-switch">
    <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault">
    <label class="form-check-label" for="flexSwitchCheckDefault">Display total after taxes</label>
  </div>
</div>


<div id="filters">
  <div class="filter"><i class="fa-solid fa-fire"></i><p>Trending</p></div>
  <div class="filter"><i class="fa-solid fa-bed"></i><p>Rooms</p></div>
  <div class="filter"><i class="fa-solid fa-mountain-city"></i><p>Iconic Cities</p></div>
  <div class="filter"><i class="fa-solid fa-mountain"></i><p>Mountains</p></div>
  <div class="filter"><i class="fa-brands fa-fort-awesome"></i><p>Castles</p></div>
  <div class="filter"><i class="fa-solid fa-person-swimming"></i><p>Amazing Pools</p></div>
  <div class="filter"><i class="fa-solid fa-campground"></i><p>Camping</p></div>
  <div class="filter"><i class="fa-brands fa-cow"></i><p>Farms</p></div>
  <div class="filter"><i class="fa-regular fa-snowflake"></i><p>Arctic</p></div>
</div> -->

<div class="filter-container">
  <div class="filter"><i class="fa-solid fa-fire"></i><p>Trending</p></div>
  <div class="filter"><i class="fa-solid fa-bed"></i><p>Rooms</p></div>
  <div class="filter"><i class="fa-solid fa-mountain-city"></i><p>Iconic Cities</p></div>
  <div class="filter"><i class="fa-solid fa-mountain"></i><p>Mountains</p></div>
  <div class="filter"><i class="fa-brands fa-fort-awesome"></i><p>Castles</p></div>
  <div class="filter"><i class="fa-solid fa-person-swimming"></i><p>Amazing Pools</p></div>
  <div class="filter"><i class="fa-solid fa-campground"></i><p>Camping</p></div>
  <div class="filter"><i class="fa-brands fa-cow"></i><p>Farms</p></div>
  <div class="filter"><i class="fa-regular fa-snowflake"></i><p>Arctic</p></div>
  <div class="filter"><i class="fa-solid fa-umbrella-beach"></i><p>Beachfront</p></div>
<div class="filter"><i class="fa-solid fa-sun"></i><p>Desert</p></div>
<div class="filter"><i class="fa-solid fa-gem"></i><p>Luxe</p></div>

  <!-- Toggle -->
  <div class="tax-toggle">
  <label class="form-check-label me-2" for="flexSwitchCheckDefault">Display total after taxes</label>
  <input class="form-check-input" type="checkbox" id="flexSwitchCheckDefault">
</div>




</div>


 <script>
  const taxSwitch = document.getElementById("flexSwitchCheckDefault");
  const taxInfoItems = document.getElementsByClassName("tax-info");

  function updateGSTVisibility() {
    for (const info of taxInfoItems) {
      info.style.display = taxSwitch.checked ? "inline" : "none";
    }
  }

  // Initial state on page load
  updateGSTVisibility();

  // Toggle on change
  taxSwitch.addEventListener("change", updateGSTVisibility);
</script>





<!-- <div class="row row-cols-lg-3 row-cols-md-2 row-cols-sm-1 mt-3">
< for(let listing of allListings) { %>
    <a href="/listings/<= listing._id %>" class="listing-link">
<div class="card col listing-card">
  <img src="<= listing.image.url %>" class="card-img-top" alt=" listing_image" style="height: 20rem">
  <div class="card-img-overlay"></div>
  <div class="card-body">
    <p class="card-text">
    <b><= listing.title %></b> <br>
    &#8377; <= listing.price.toLocaleString("en-IN") %> / night
    </p>
  </div>
</div>
</a>
< } %>
</div> -->

<div class="row row-cols-lg-3 row-cols-md-2 row-cols-1 mt-3">
  <% for(let listing of allListings) { %>
    <div class="col mb-4">
      <a href="/listings/<%= listing._id %>" class="listing-link text-decoration-none text-dark">
        <div class="card listing-card h-100">
          <img src="<%= listing.image.url %>" class="card-img-top" alt="listing image" style="height: 20rem">
          <div class="card-body">
            <p class="card-text">
              <b><%= listing.title %></b><br>
              ₹<%= listing.price.toLocaleString("en-IN") %> / night
              <i class="tax-info">&nbsp; &nbsp; +18% GST</i>
            </p>
          </div>
        </div>
      </a>
    </div>
  <% } %>
</div>

